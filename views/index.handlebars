<h1>dogs4sale!!</h1>

<h2>Dogs that have been adopted</h2>

<ul>
  {{#each dogs}}
    {{#unless adopted}}
      {{> dogs/dog-block adopted=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Dogs open for adoption</h2>

<ul>
  {{#each dogs}}
    {{#if adopted}}
      {{> dogs/dog-block adopted=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Dog</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Dog Name:</label>
    <input type="text" id="ca" name="name">
    <label for="ca">Dog Breed:</label>
    <input type="text" id="ca" name="name">
  </div>


  <button type="submit">Add Dog</button>
</form>
